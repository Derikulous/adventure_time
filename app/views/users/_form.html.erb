<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="well col-lg-13">
  <div>
    <%= f.label :name %><br />
    <%= f.text_field :name, :class => "form-control", :placeholder => "Abraham Lincoln" %>
  </div>
  <div>
    <%= f.label :email %><br />
    <%= f.text_field :email, :class => "form-control", :placeholder => "abegoesham@gmail.com" %>
  </div>
  </br>
    <div>
    <%= f.submit "Update", :class => 'btn btn-primary' %>
  </div>
</div>

<% end %>

